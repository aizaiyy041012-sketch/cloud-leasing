// Official leasing system URL. Every "Get Started" / "Lease Now" / "Start Now" style CTA
// site-wide reads this value rather than hardcoding the URL, so the real address only
// needs to be swapped in once, here.
window.LEASING_SYSTEM_URL = "https://app.aicloudleasing.com/";
