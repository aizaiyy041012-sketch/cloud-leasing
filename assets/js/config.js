// TEMPORARY placeholder link — will be replaced with the official leasing system URL later.
// Every "Get Started" / "Lease Now" / "Start Now" style CTA site-wide should read this value
// rather than hardcoding the URL, so the real address only needs to be swapped in once, here.
window.LEASING_SYSTEM_URL = "https://vig-compute.net/vig/index.html#/";
